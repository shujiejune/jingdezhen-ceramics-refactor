# Plan: `frontend/TODO.md` — milestone plan for the production frontend build

**Scope of this step:** write ONE new file, `frontend/TODO.md`, containing the full milestone plan as a markdown TODO list. No code changes. Milestones mirror the PRD §7 tracks (M0–M4) prefixed `M-F*` so progress can be tracked alongside the backend in `docs/REFACTOR-TODO.md`, plus a user-driven design track (M-FD) honoring "I'll revise the UI later". The plan is grounded in the actual backend contract (inventoried today) — not the mock assumptions.

Full file content:

---

# Frontend Build Plan — TODO

> Takes the hi-fi prototype (commit `7ca45e3` + magazine UI pass) to production per `frontend/AGENTS.md`, TDD §6/§7/§5.3, PRD §3–§4/§7. Milestones mirror PRD §7 tracks, prefixed `M-F*` for the frontend; `M-FD` is the design-revision track. Track completion here + mirror in `docs/REFACTOR-TODO.md` → "Frontend".
> Status: ☐ todo · ◐ partial · ☑ done

## M-FD — Design revision track (user-driven, runs in parallel)
- [ ] Heritage index → horizontal magazine chapters (spine chrome) — reuse `useLoopScroller`
- [ ] Gallery → museum-window / record-crate browsing (freakmag-style)
- [ ] Apply user's post-review UI tweaks (panel pacing, parallax, spine density, family-color saturation)
- [ ] Guard `tokens.css` ↔ `tailwind.config.ts` hex/radius twins stay in sync

## M-F0 — Foundations & tooling (no backend dependency)
- [ ] ESLint (typescript-eslint, react-hooks) + Prettier; `lint`/`format` scripts
- [ ] Vitest + React Testing Library setup
- [ ] Unit tests: money formatting, i18n catalog key-parity, `useLoopScroller` wrap math, ApiError adapter, seededRandom determinism
- [ ] ErrorBoundary + 500 page; loading/error component-state audit
- [ ] Responsive + a11y baseline (focus rings, keyboard nav for spine/loop, aria)
- [ ] Frontend CI job in `.github/workflows/ci.yml` (pnpm → lint → typecheck → vitest → build)
- [ ] `frontend/README.md` quickstart (dev modes, demo accounts)
- **Acceptance:** CI green; `lint && typecheck && test && build` clean

## M-F1 — Live API integration (storefront)
Contract realities from the backend inventory (absorb ALL of these):
- [ ] LiveTransport: flat bodies; `PaginatedResponse`; `{data:[...]}` wrappers; CSV/302 via direct links
- [ ] ApiError adapter for the real envelope — `{"message"}` today (+ Fiber plain-text 404/405), 401 `{"error":{code,message,pending_token}}` for 2FA; 204 = success (analytics, mark-read)
- [ ] Dev proxy `/api/*` → Fiber:1323 (strip prefix) + `/media` passthrough; origin-prefix helper for relative media URLs
- [ ] TanStack Query + SSR-loader hydration; migrate catalog/product/content/cart/wishlist reads
- [ ] Real auth: signup 201 `access_token:""` → activation page (`POST /auth/activate`); forgot/reset pages; Google receiver routes `/login/success|/login/2fa|/login/2fa/enroll|/login/error`
- [ ] 2FA complete: verify, pending-enroll (QR/secret), pending-confirm (backup codes)
- [ ] `?locale=&currency=` on all reads; cookie↔context sync
- [ ] Keep `mock` mode as dev/test fixture; contract tests for both envelopes
- **Acceptance:** full journey vs `make up` backend, zero mock imports

## M-F2 — Commerce & account depth
- [ ] Real media: `/:id/media` galleries in detail pages; srcset/lazy (OSS process params); hls.js video (may-trail)
- [ ] Address book CRUD + default toggle
- [ ] Checkout live: reactive quote UX (unshippable/overweight), `hosted_url` redirect, order polling → `paid`
- [ ] Order timeline, cancel-unpaid, tracking; certificate QR + PDF (302, 404-not-generated state)
- [ ] Itinerary: quote view/PDF, `pay-deposit` + poll `deposit_paid`, my-journeys statuses
- [ ] Account: profile, 2FA enroll/backup codes, consent history, GDPR export/delete
- [ ] Notifications: list + unread badge + mark-read (poll; WS push in M-F5)
- **Acceptance:** sandbox payment OK in USD/EUR/GBP; deposit flow reaches `deposit_paid`

## M-F3 — Compliance, SEO & analytics
- [ ] Cookie consent banner → public `POST /consent` (granular kinds, doc_version); re-open from footer
- [ ] Analytics: `pageview` on route change + `itinerary_form_view` on wizard mount (funnel contract); 204 = dropped, no retry
- [ ] SEO: hreflang from `alternates`, canonical/OG/Twitter per entity, JSON-LD completion, sitemap cross-link
- [ ] Policy placeholder pages (privacy/terms/cookies)
- **Acceptance:** SEO audit per PRD §4.4 both locales; consented events reach dashboard

## M-F4 — Admin CMS (client-rendered, RBAC)
- [ ] Admin shell: `/$locale/admin` client-rendered; staff guard; permission-keyed nav; 2FA path
- [ ] Adopt TanStack Table + TanStack Form/zod
- [ ] Content CMS ×4 (story/engage/artist/product): bilingual edit, tags, submit; super-admin approve/reject/unpublish
- [ ] Media library: presign/upload/registry; per-entity galleries (attach/detach/reorder)
- [ ] Products/SKUs: CRUD, attributes editor, bulk CSV import UX (BulkImportSummary report)
- [ ] Orders: ship (carrier+tracking), complete, full refund
- [ ] Itinerary CRM: inbox (status/assignee/SLA), assign, notes, quote send (line_items, pay_full), confirm, refund-deposit, CSV
- [ ] Settings: shipping tiers, option rates, FX refresh
- [ ] Dashboard: traffic/sales/funnel + range presets + CSV; users & roles; audit log (+CSV); certificates admin
- **Acceptance:** per-role module visibility; editor→submit→publish round-trip live

## M-F5 — Chat, polish & launch readiness
- [ ] Chat widget + agent console — **backend-blocked**; build vs mock frames first
- [ ] WS notification push (same blocker) with reconnect/backoff; poll fallback until then
- [ ] Performance: admin code-split, image audit, LCP < 3s (PRD §4.2)
- [ ] WCAG 2.1 AA pass (public pages)
- [ ] Playwright E2E: browse→cart→checkout(mock), signup+activate, locale switch, wizard submit, certificate verify
- [ ] Deploy: node-server preset; Caddy (`/api/*` strip, `/media`, `/ws`); env matrix; smoke checklist
- [ ] Docs sync: AGENTS.md as-built, REFACTOR-TODO, TDD §12
- **Acceptance:** staging smoke green; E2E suite green

## Backend dependencies (external — root `docs/REFACTOR-TODO.md`)
- `/ws` browser auth (query-token/subprotocol) — blocks WS push + chat
- Chat sessions/frames + agent endpoints (TDD §5.3)
- Central error-mapper with stable `code`s (TDD §4.3) — M-F1 ships message-mapping adapter meanwhile
- CORS `CLIENT_ORIGIN` for the frontend origin in dev compose

## May-trail (PRD §7 MLS)
Google/WhatsApp OAuth providers (receiver routes ship in M-F1) · live payments · WhatsApp messaging · chat beyond minimal · legal-reviewed policies · bulk-import UX polish · hls.js video

---

**After writing the file:** also tick the docs cross-reference (one line under `docs/REFACTOR-TODO.md` → Frontend pointing to `frontend/TODO.md`) — no other changes.