# Backend Refactor TODO

Tracking the transition of the existing `backend/` codebase (former "Learning & Communication Platform") to the PRD targets (`docs/PRD.md`, v0.17). Milestone references (M0–M4) per PRD §7.

## ✅ Done — Deleted redundant modules (not in PRD)

- [x] `internal/modules/course` + `models/course.go`, `models/quiz.go`, `models/assignment.go` (online courses, quizzes, assignments)
- [x] `internal/modules/forum` + `models/forum.go` (community forum)
- [x] `internal/modules/note` + `models/note.go` (private study notes)
- [x] `internal/modules/portfolio` + `models/portfolio.go` (student portfolio works)
- [x] `models/badge.go` + badge fields in `ProfileData` (gamification)
- [x] Migrations: 000001, 000003, 000012–000033, 000035–000036, 000038
- [x] Cleaned dangling references: `main.go`, `router.go`, gallery (note deps), notification types, `Artwork.NoteCount`
- [x] `go build ./...` and `go vet ./...` pass

## M0 — Foundations

### Migration baseline
- [x] Squash remaining migrations (000002, 000004–000011, 000034, 000037) into a fresh `000001_baseline` (pre-production, gaps in numbering)
  - Fixes applied during squash: `events` table renamed to `activities` (code queries `activities`); `ceramic_stories.slug` column added (model/repo use it but no migration created it); `articles.id` changed to `BIGSERIAL` (was plain `BIGINT` with no generator); tables reordered so FK targets exist (`artworks` before `artwork_images`)
  - ⚠️ Existing dev databases must be reset (`migrate drop` or recreate the DB) — the old `schema_migrations` version numbers no longer exist
  - ⚠️ Known bug spotted (not fixed, pre-existing): `cs_repository.go` `ORDER BY … start_year DSC` — typo, should be `DESC`; will fail at query time
- [x] Validate baseline against real PostgreSQL (no Docker available in this session) — first `make migrate-up` or testcontainers run
- [x] Switch migration workflow into CI (run against testcontainers, PRD §2.4.1) — covered by the integration job in `.github/workflows/ci.yml`: `go test -race` runs the testcontainers harness which applies ALL migrations (embedded via `//go:embed *.up.sql`) against a real PG container per test. Schema drift fails the pipeline. (The `migrate` CLI Makefile target remains for dev ops; CI uses the Go API.)

### User / Auth (PRD §3.4.1, §3.5)
- [x] Replace role model `admin/normal_user/guest` with PRD RBAC: customer + `super_admin`, `content_editor`, `travel_planner`, `ecommerce_operator`, `customer_service` (roles/permissions tables) — migration `000002_rbac`, models/rbac.go, middleware `RequirePermission`/`RequireRole`/`HasRole`, repo `GetUserRoles`/`AssignRole`, service `AdminAssignRole`, admin user routes wired under `/admin/users`
- [ ] Add shipping address book (multiple addresses per user; country drives shipping calculator)
  - [x] Shipping address book implemented: migration `000003_user_addresses`, `models/address.go`, `address` module (handler/service/repository), routes under `/profile/addresses`; one-default-per-user enforced via partial unique index + tx
- [x] Add preferred locale (`en-US`/`zh-CN`) and preferred currency (USD/EUR/GBP) to profile — migration `000004_user_preferences` adds columns + CHECK constraint; `User`/`UserUpdateData` updated; all 4 scan paths fixed (latent double-ProfileData scan bug removed); validator `oneof=USD EUR GBP` for clean 400s
- [x] Add consent records (Privacy Policy / ToS acceptance with timestamp & version — GDPR) — migration `000005_consent_records` (append-only, nullable user_id for anonymous, 4 kinds, IP hashed via HMAC); `models/consent.go`; `consent` module (repo/service/handler); `POST /consent` public, `GET /profile/consent` + `GET /profile/consent/:kind` protected; `CONSENT_HMAC_KEY` in config
- [x] Add TOTP 2FA (mandatory for Super Admin, optional for staff) 
  — migration `000006_user_2fa` (encrypted secret at rest via AES-GCM, two-phase enroll→confirm); `pkg/utils/crypto.go` AES-GCM; `twofa` module (repo/service/handler); login gates on enabled 2FA via `Err2FARequired` + 5-min pending JWT, `/auth/2fa/verify` completes login; `POST /profile/2fa/{enroll,confirm}`, `DELETE /profile/2fa`; `TWO_FA_ENCRYPTION_KEY` in config. 
  - **Super-admin must-enroll enforced**: a super_admin with no 2FA is blocked at login (`Err2FAEnrollmentRequired`), walked through `/auth/2fa/pending-enroll` → `/auth/2fa/pending-confirm` (15-min pending token; confirm enables 2FA AND mints the real JWT). 
  - **Google OAuth login is 2FA-gated too** (shared `challenge2FAOrMint` helper). 
  - **Backup codes**: migration `000009_user_2fa_backup_codes` (one-time, stored SHA-256 hashed with app-key pepper); generated at confirm (shown ONCE), accepted at `/auth/2fa/verify` (TOTP tried first, then a backup code is consumed atomically); `POST /profile/2fa/backup-codes/regenerate` + `GET /profile/2fa/backup-codes` (remaining count); `Disable` clears backup codes; unit tests for hashing/normalization
- [ ] Add WhatsApp OAuth provider (schema already generic via `auth_provider`) — *may trail post-MVP*
- [x] Replace AWS SES sender with **Brevo** (keep `email.ServiceInterface`; remove AWS config/deps) 
  — `pkg/email/brevo_sender.go` REST adapter; `ses_sender.go` removed; AWS SDK dropped from go.mod via `go mod tidy`
- [x] Extend `config.go`: Brevo, Airwallex, PayPal, Qwen, OSS, Redis; remove AWS fields — `.env.example` documents all keys; Redis added to `docker-compose.dev.yml`
- [x] GDPR self-service: user data export + account deletion endpoints 
  — migration `000010_user_gdpr` (adds `users.deleted_at`; anonymize-in-place so order-record retention keeps referential integrity); `models/privacy.go` (`UserDataExport` bundle + `DeleteAccountRequest`); `privacy` module (repository spans users/addresses/consent/2fa/favorites/notifications tables; service captures email pre-erasure then anonymizes + enqueues Brevo confirmation via `email:send` queue); `GET /profile/export` (synchronous JSON, `Content-Disposition` download; async job + download link deferred to M2/M3 when order history makes payload large); `POST /privacy/delete-account` (`{"confirm":"DELETE"}` body guard; irreversibly nulls email/nickname/password_hash/avatar/tokens/auth_provider_id, sets `is_active=false`+`deleted_at`; CASCADE-purges addresses/2FA/favorites/notifications; SET NULL on consent_records + content authorship for audit). Login already rejects erased stubs via `is_active=false`. JWT blocklist (deleted-user token invalidation) deferred to M4 security pass — DONE: see `pkg/adapters/tokenblocklist/` below

### Infrastructure
- [x] Add Redis (sessions, cache, pub/sub, Asynq job queue) — `redis:7-alpine` in `docker-compose.dev.yml` (port 6379, healthcheck); `internal/platform/redis` go-redis client; `internal/platform/jobs` Asynq enqueue client + worker server + cron scheduler; `main.go` split into `serve`/`worker` run modes; compose runs both
- [x] Add OSS media upload pipeline (WebP conversion via OSS image processing) — **adapter + media_assets schema landed** (see Media library TODO above); WebP on-the-fly conversion via OSS image-processing params (`?x-oss-process=image/format,webp`) deferred until OSSStore is live + CDN configured
- [x] i18n content infrastructure: per-locale translation tables pattern (BCP 47 keys) — `platform/i18ncontent` package (locale constants en-US/zh-CN, `ContentStatus` workflow enum, rich-content `ContentBlock` types, `NormalizeLocale`, `Transition`/`CanEdit` state machine, `PublishedFilter`); `models/i18n.go` shared types; unit test for the workflow transition matrix. Per-module migrations (ceramicstory/engage/artist) follow
- [ ] Rewrite `ws` hub with Redis pub/sub fan-out + chat session/message persistence (PRD §3.3.1)

## M1 — CMS & Content

### Restructure kept content modules (i18n + workflow)
- [x] `ceramicstory` → History & Heritage: add translations, rich multimedia blocks, workflow status (draft → review → published), `created_at`/`updated_at` — migration `000007_ceramic_story_translations`; admin CMS wired (create/update/delete/submit/approve/reject/unpublish) under `/admin/ceramicstory` with `RequirePermission(PermContentWrite)` + `PermContentPublish` split; service uses `i18ncontent.Transition`/`CanEdit`/`NormalizeLocale(validate=true)`. Rich-content blocks + media gallery deferred
- [x] `engage` (`Activity`/`Article`) → Destinations & Local Lifestyle: add translations, per-locale slugs/meta, media galleries, OSM coordinates, workflow status — migration `000008_activity_translations`; admin CMS wired (create/update/delete/submit/approve/reject/unpublish) under `/admin/engage` with `RequirePermission(PermContentWrite)` + `PermContentPublish` split. Media galleries + opening_info modeling deferred
- [x] Artist model → full artist profiles (bio/media, i18n, linked to products) — migration `000011_artist_translations` (parent gains `avatar_url` + `display_order`; translation table with name/slug/bio/meta + workflow status, UNIQUE(locale,slug), hot-path indexes); backfilled en-US from existing `artists.name`/`bio` (slug derived from name); `models/artist.go` merged view + `Create`/`Update` DTOs; dedicated `artist` module (handler/service/repository following ceramicstory pattern); public reads `GET /artists` + `GET /artists/:slug` (locale-aware, published-only, paginated); admin CMS under `/admin/artists` with `RequirePermission(PermContentWrite)` + `PermContentPublish` split; old `name`/`bio` parent columns retained (additive, future cleanup with gallery evolution)
- [x] Product tags (translatable) — migration `000021_product_tags`: evolves the dead baseline `tags(name)` table into `tags(id, key UNIQUE)` (language-neutral canonical key, lowercase kebab-case CHECK) + `tag_translations(tag_id, locale, name)` + `product_tags(product_id, tag_id)` join. PRD §3.2.1 line 173 ("Category/tag browsing, filtering, sorting, and keyword search"); TDD §3.2 line 130 (`tags(id) + tag_translations(name)`). Tags are **taxonomy, not editorial content** — no draft/publish workflow on tag_translations (unlike artist/product translations); a tag is visible iff attached to a published product. Public reads JOIN tag_translations for the requested locale with **en-US → key fallback** (COALESCE, never blank). Admin assigns tags by **canonical key** (not id/name): `SetProductTags` resolves-or-creates keys inline (`INSERT ON CONFLICT (key) DO UPDATE ... RETURNING id`) + seeds an en-US name=key default for new tags (operator edits later) + absolute-replaces the `product_tags` set (nil=unchanged, `[]`=clear). Wired into `CreateWithTranslation` + `UpdateTranslation` txs. `CreateProductData.Tags []string` / `UpdateProductData.Tags *[]string` / `BulkImportRow.Tags []string` (semicolon-separated in CSV). `FindTagsByProductIDs` batch-loads (map[productID][]Tag, no N+1) attached in list + detail + create/update responses. `FindAllTagsInUse` lists tags with locale name + published-product count. Endpoints: `GET /catalog/tags?locale=` (facet list), `GET /catalog/products?tag=key1,key2` (ANY-match filter, comma-separated), `GET /admin/products?tag=` (admin filter), tags in create/update/bulk-import payloads. Seed `35_product_tags.sql` (6 tags × 2 locales + 10 attachments). `models/tag.go` evolved (added `Key`); dead `TagWithPostCount` (inherited forum code) removed. **Category tree stays deferred** (bare `products.category` string unchanged); tags are an orthogonal discovery facet.
- [ ] Approval workflow: only Super Admin can approve/publish (PRD §3.1.1)
- [x] Media library (OSS upload, WebP, FFmpeg→HLS video) — **foundation landed**: migration `000020_media_assets` (`media_assets(id, kind image|video, oss_key UNIQUE, mime, width, height, duration, hls_key NULL, uploaded_by, created_at, updated_at)` central registry + `product_media(product_id, media_id, sort_order, caption, UNIQUE(product_id,media_id))` ordered gallery; `pkg/adapters/storage/` `Store` interface (`PresignUpload`/`PublicURL`/`Put`/`Delete`/`Key`) + `LocalStore` (dev: writes to `STORAGE_LOCAL_DIR`, served via Fiber static mount at `/media`) + `OSSStore` (live Alibaba Cloud OSS, HMAC-SHA1 v1 presign, NOT live-tested until creds land — same approach as Airwallex/PayPal); `models/media.go`; `media` module (handler/service/repository); upload flow (TDD §2.2): `POST /admin/media/presign` (returns presigned PUT URL for OSS, or local-mode marker) → browser uploads direct to storage → `POST /admin/media/assets` registers the `oss_key` (local dev: `POST /admin/media/upload` multipart `Put`'s server-side); product gallery CRUD `/admin/products/:id/media` (attach/detach/reorder/list, `product.write`) + public `GET /catalog/products/:id/media`; product detail surfaces `gallery []ProductMediaItem` (first item's `public_url` becomes `thumbnail_url`, fallback to old column); `STORAGE_MODE=local|oss` env flip; `media:transcode` job type registered (no-op handler — FFmpeg unavailable, videos deferred); URL-passthrough for full-URL `oss_key` (seeded/external assets). Seed `65_media.sql` (5 placeholder images attached to the 5 products). `artist_media`/`ceramic_story_media` are mechanical follow-ups. **OSSStore not live-tested** (no creds); **video transcode no-op** (FFmpeg); **OSS WebP image processing deferred** (needs OSS image-processing params on PublicURL). Old `products.thumbnail_url` kept dead (gallery preferred, fallback retained).
- [ ] SEO: per-locale slugs, meta tags, sitemap.xml generation, hreflang
- [ ] Admin CMS routes rebuilt around RBAC (router `/admin` group is currently a placeholder)

## M2 — E-commerce (all new)

- [x] Product/SKU model on top of `artworks`: price (CNY), stock, packed weight, JSONB attributes (size, technique, glaze, edition type, year, kiln — PRD §3.2.1), publish status, product videos — migration `000012_products_skus` (parent `products` + `product_translations` i18n table with editorial workflow; `skus` table with price_cny BIGINT minor units, stock, weight_grams, low_stock_threshold default 2, attributes JSONB, GIN index; backfilled from `artworks` + en-US translations + default SKU per product; added `product.publish` permission for super_admin-only publish gate); `models/product.go` (`Product` merged view + `SKU` + DTOs); `product` module (handler/service/repository following the ceramicstory/artist pattern); public reads `GET /catalog/products` + `GET /catalog/products/:slug` (includes SKUs); admin CMS under `/admin/products` with `RequirePermission(PermProductWrite)` + `PermProductPublish` split; SKU CRUD under `/admin/products/:id/skus` (create) + `/admin/skus/:id` (update/delete). Gallery module (`artworks`) left intact (additive; catalog is parallel). Product videos + media_assets FK deferred to media-library infra
- [x] Evolve `user_favorite_artworks` into **wishlist** — migration `000013_evolve_artworks_drop` creates `wishlists(user_id, sku_id, created_at)` (keyed on SKU, the purchasable unit — a customer favorites a variant, not a product); migrates existing favorites via the default-SKU mapping from 000012; drops the legacy `user_favorite_artworks` + `artworks` + `artwork_images` + `artwork_tags` tables; dedicated `wishlist` module (handler/service/repository) with `GET /wishlist` (locale-aware, enriched with product display info via JOIN skus→products→product_translations), `POST /wishlist` (`{"sku_id":N}`), `DELETE /wishlist/:sku_id`. Gallery module + `models/artwork.go` deleted; products is now the sole catalog read path
- [x] Cart (server-side, merge-on-login; quantity ops, bulk remove) — migration `000015_carts` (`carts.user_id UNIQUE` → one cart per signed-in user; `cart_items(cart_id, sku_id, qty)` with `UNIQUE(cart_id,sku_id)`; `ON DELETE CASCADE` mirrors wishlists); `models/cart.go`; `cart` module (handler/service/repository). Endpoints: `GET /cart` (locale-aware, `?currency=` presentment via FX); `POST /cart/items` (additive — existing qty += new, qty defaults to 1); `PATCH /cart/items/:sku_id` (absolute set); `DELETE /cart/items/:sku_id`; `DELETE /cart/items` (bulk `{"sku_ids":[...]}`); `POST /cart/merge` (guest localStorage cart → server cart on login; additive, capped at stock, unknown SKUs skipped). Stock is NOT decremented at cart stage (authoritative atomic decrement at checkout, TDD §4.3); service enforces an advisory `qty>stock` guard (`ErrConflict`). CNY totals always present; presentment totals + per-line presentment when `?currency=` supplied (graceful degradation → CNY-only on FX failure). Seed `60_cart.sql` (customer, 2 items)
- [x] FX pipeline: daily ECB rates + 2% markup + rounding rule — migration `000014_fx_rates` (`fx_rates(currency CHAR(3) PK, rate_to_cny NUMERIC(18,8), fetched_at)`); `internal/platform/fx` package (`RateSource` interface, `ECBClient` XML parser, `FixtureRateSource` for dev/tests, pure `Convert`/`RoundPrice` functions, `Repository` for fx_rates, `Service` with `Refresh` + `Convert`); worker `fx:refresh` job wired (`jobServer.FXRefresh = fxService.Refresh`); `POST /admin/fx/refresh` (settings.manage) enqueues the job; `GET /fx/rates` debug endpoint; `GET /catalog/products/:slug?currency=USD|EUR|GBP` consumer adds `price`+`price_currency` to each SKU (graceful degradation — CNY-only if rates missing). Rounding rule (PRD §3.2.3): `<100` → ceil to 0.50; `≥100` → ceil to 1.00. Markup direction: stored rate = raw ÷ (1+markup), so customer pays more presentment per CNY. Unit tests for RoundPrice matrix + Convert + Refresh markup direction (priority test target, TDD §11). `shopspring/decimal` dep for money arithmetic (never float, TDD §7). Redis cache TTL + stale-rate alert (>48h) deferred
- [x] `ShippingFeeTable`: per-country weight tiers; fee calculator; overweight block + contact message — migration `000016_shipping_fee_tiers` (`shipping_fee_tiers(country CHAR(2), max_weight_grams, fee_cny, UNIQUE(country, max_weight_grams))`; seeded US/GB/DE/CN tiers. `internal/platform/shipping/calc.go` pure `CalcFee(tiers, weight)` (no tier → `ErrUnshippable`; weight > heaviest tier → `ErrOverweight`; else cheapest sufficient tier) + unit tests (boundary/overweight/unshippable). `shipping` module: `GET /shipping/quote?country=&weight=` public preview (TDD §5.2) + `/admin/shipping/tiers` CRUD (settings.manage).
- [x] Checkout (signed-in only) + order lifecycle (created → paid → shipped → completed / cancelled / refunded, full refunds only) — migration `000017_orders` (`orders` with presentment + CNY totals, `fx_rate_used` NUMERIC snapshot, immutable `address` JSONB snapshot, status state machine with CHECK; `order_items` with `unit_price_minor` + `unit_price_cny` + `title_snapshot`/`attributes_snapshot` JSONB; `user_id` FK NO ACTION — orders survive GDPR erasure). `models/order.go`; `order` module (handler/service/repository). Authoritative stock decrement inside the order-creation tx (TDD §4.3: `UPDATE skus SET stock=stock-$1 WHERE id=$2 AND stock>=$1`; zero rows → rollback → `ErrConflict`, no partial order). Order snapshot reconciles exactly (line=unit×qty, subtotal=Σline, total=subtotal+shipping) — matches cart convention + checkout snapshot model (TDD §7). Routes: `POST /checkout`, `GET /orders[/:id]`, `POST /orders/:id/cancel` (created→cancelled, stock restored); admin `GET /admin/orders[/:id]` (order.read), `POST /admin/orders/:id/ship` (order.write, paid→shipped with carrier+tracking), `/complete` (shipped→completed), `/refund` (order.refund, paid|shipped→refunded, full refunds only). Mock payment seam (`PAYMENTS_MODE=mock` dev default): checkout enqueues `payment:finalize{success}` → worker `MarkPaid` drives created→paid; live mode (Airwallex/PayPal adapters) is #6. `fx.Service.Rate` added for the `fx_rate_used` snapshot. `jobs.PaymentFinalizePayload` extended with `OrderID`+`Success`. User service `PreferredCurrency` added (checkout default currency). Config `PAYMENTS_MODE`
- [x] Airwallex integration (Payment Intents + webhooks) — **sandbox for MVP** — `pkg/adapters/payments` Gateway interface (TDD §10: CreateIntent/Refund/VerifyWebhook) + `AirwallexGateway` (HTTP client: `/pa/payment_intents/create`, `/pa/refunds/create`, HMAC-SHA256 webhook verify over the raw body, `/authentication/login` bearer token). `payment` module (repo/service/handler + `Registry`). `payments` table (migration 000018: gateway, gateway_ref, status pending|succeeded|failed|refunded, amount_minor, currency, raw_webhook JSONB, idempotency_key UNIQUE). Checkout in sandbox/live creates an intent + returns `hosted_url` (hosted checkout); the webhook verifies the signature → idempotent upsert (ON CONFLICT idempotency_key, xmax=0 probe) → enqueue `payment:finalize` → worker `MarkPaid` (idempotent — replayed webhook is a 200 no-op). Unit-tested with httptest stubs (CreateIntent/Refund/verify valid+tampered); live sandbox round-trip pending merchant onboarding (post-MVP, PRD §2.1)
- [x] PayPal integration (checkout + webhooks) — **sandbox for MVP** — `PayPalGateway` (Orders v2 `CreateIntent` → approve link; `/v2/payments/captures/{id}/refund`; `/v1/notifications/verify-webhook-signature` API; client-credentials bearer cached). Same `payments.Gateway` interface + `payments` table + webhook path. Unit-tested with httptest stubs; live sandbox pending onboarding. **Note:** the Airwallex/PayPal HTTP clients are written from the public sandbox API specs and unit-tested with httptest + fixture signatures but NOT live-sandbox-tested (no merchant creds until post-MVP onboarding); `mock` mode (`PAYMENTS_MODE=mock`, dev default) is the fully-tested path
- [x] Certificates: auto-generate at product creation, QR public page, provenance records, PDF — migration `000019_certificates` (`certificates(product_id UNIQUE, cert_code UNIQUE, qr_key, pdf_key, issued_at)` + `provenance_records(certificate_id, kind created|sold|transferred, detail JSONB, at)`; seeded `certificate.manage` perm). `pkg/adapters/certchain/` (`Chain` interface + `NoopChain` — reserved blockchain adapter, PRD §5.4, vendor plugs in later). `models/certificate.go`. `certificate` module (handler/service/repository). `product.AdminCreateProduct` auto-issues via injected `CertificateIssuer` (best-effort — a cert error doesn't fail product creation; operator can regenerate). `order.MarkPaid` appends a `sold` provenance via injected `ProvenanceRecorder` (best-effort, logged+skipped; the cert is the source of truth, a missed row is backfillable). Routes: `GET /certificates/:code` (public QR-target page: product + artist + provenance chain, locale-aware) + `GET /certificates/:code/qr` (on-demand PNG via `skip2/go-qrcode`, no OSS storage needed — `qr_key` nullable until storage adapter lands) + `/admin/certificates` (list/get/regenerate). cert_code = `JDZ-<6-base32-crypto-rand>` with UNIQUE-collision retry. **PDF deferred** (`pdf_key` nullable, `pdf:generate` job stays no-op) pending the TDD §12 engine decision (chromedp vs gofpdf) + the OSS storage adapter; the cert_code + on-demand QR + public page + provenance is the MVP scannable-certificate delivery. Seed `80_certificates.sql` (5 products). Dep `github.com/skip2/go-qrcode`
- [x] Bulk product import (CSV) + low-stock alerts (threshold default 2, dashboard + Brevo email) — **no migration** (`low_stock_threshold INT DEFAULT 2` already on `skus`, notifications + roles already exist). **Bulk import** (PRD §3.4.1 line 175): `POST /admin/products/import` (multipart CSV or raw body, `product.write`); handler parses with `encoding/csv` (stdlib, no dep), service `AdminBulkImport` creates each row in its own tx (partial-success: a bad row doesn't poison good ones) + best-effort cert auto-issue, returns `{imported, failed, results[]}` per-row report. One product per row (+ optional first SKU via `sku_code`); multi-SKU products use the regular per-product SKU endpoint. CSV columns mirror the create DTOs. **Low-stock alerts** (PRD §3.4.1 line 176, TDD line 234 `stock:check | order paid`): on `MarkPaid` (worker), enqueue `stock:check{order_id, sku_ids}` (event-driven, NOT cron — fires once per actual stock change); worker handler `FindLowStock(sku_ids)` (stock <= low_stock_threshold) → for each low SKU, `CreateNotification(low_stock)` + `EnqueueEmailSend` to every `ecommerce_operator` user (`ListByRole`). Best-effort: a notification/email failure is logged, never blocks the order. `StockCheckPayload` + `EnqueueStockCheck` added to jobs; `StockCheckEnqueuer` interface + setter on order.Service (wired in both serve + worker — MarkPaid runs in worker); `ListByRole` added to user repo; `FindLowStock` added to product repo; `NotificationTypeLowStock` added; fixed pre-existing `notifications.db:"notification_id"` tag mismatch (column is `id` — `pgx.RowToStructByName` now resolves). Dashboard *stats* endpoint stays a separate TODO; this is the notification + email alert path.
- [x] Order emails via Brevo (order-confirmation enqueued at checkout; transition emails for paid/shipped/refunded are next); tracking-number entry (manual, no carrier APIs) — `POST /admin/orders/:id/ship` records carrier_name + tracking_number (PRD §3.2.3: no carrier API integration). Order-confirmation email enqueued at checkout via the `email:send` job; paid/shipped/refunded transition emails deferred to the payments+email-template pass (#6)

## M3 — Custom Travel & Support (all new)

- [x] Itinerary 4-step wizard API (customer-facing) — migration `000022_itinerary_requests` (`itinerary_requests` with the full 4-step form snapshot + status state machine + `sla_deadline` + `assigned_to`; `user_id` FK NO ACTION — survives GDPR erasure, mirrors orders; `itinerary_drafts` one-per-user `UNIQUE(user_id)` for save & resume). `models/itinerary.go` (`ItineraryRequest`, `ItineraryDraft`, `ItinerarySubmitRequest` DTO with `validator` tags — `pace`/`channel`/`guide`/`hotel_level` oneof, `consent` must be `eq=true`, `arrival_date` YYYY-MM-DD, money as minor units). `itinerary` module (handler/service/repository). **Endpoints**: `GET/PUT/DELETE /itineraries/draft` (save-resume, one per user via `ON CONFLICT DO UPDATE`); `POST /itineraries` (submit — validates consent gate, computes `sla_deadline = now + 24h`, inserts request + deletes draft in one tx, best-effort records `privacy_policy` consent via injected `ConsentRecorder` + enqueues 24h-SLA ack email via `EmailEnqueuer`); `GET /itineraries` (paginated, own only); `GET /itineraries/:id`; `POST /itineraries/:id/cancel` (CAS `pending→cancelled` scoped to user — not-owned → `ErrNotFound`, non-pending → `ErrItineraryNotCancellable`). Injected interfaces (narrow, satisfied by existing services, wired in main.go): `EmailEnqueuer` (reuse `orderEmailEnqueuer`), `UserFetcher` (`userService`), `ConsentRecorder` (`itinConsentRecorder` adapter over `consent.Service`). RBAC perms (`itinerary.read/write/confirm`) already existed + granted in `000002_rbac`. Seed `85_itineraries.sql` (2 requests: pending + cancelled; 1 draft). Integration tests: `TestUpsertDraft_OnePerUser` (second save replaces, not duplicates), `TestSubmit_DeletesDraftInTx`, `TestCancelRequest_CAS` (owner ok / not-owned 404 / re-cancel 409), `TestSLADeadline_SetOnCreate`. **Scope note**: this is the customer submission flow only — the planner CRM (inbox/assign/notes), quote builder + `option_rates`/`itinerary_quotes`, deposit payment via the payments stack, and itinerary PDF (chromedp) are the next M3 sub-tracks.
- [x] 24h-SLA acknowledgment email (Brevo) — ack email on submit (commit 5489c7d); SLA breach flagging wired in the CRM sub-track below
- [x] Planner CRM: inbox, statuses, assignment, notes, CSV export — migration `000023_crm_notes` (`crm_notes` table + `sla_notified_at` for exactly-once breach notification + partial breach index). `itinerary` module extended with admin repo/service/handler methods. **Endpoints**: `GET /admin/itineraries` (inbox, filters: `status`/`assigned_to` incl. `unassigned`/`sla` — `sla_breached`/`sla_approaching`/`sla_on_time`/`sla_met` derived in SQL against `sla_deadline`+NOW()); ordered by SLA urgency then submission), `GET /admin/itineraries/:id` (detail with customer email/nickname JOINed), `GET /admin/itineraries/planners` (assignment dropdown — active travel_planner users via `user.Service.ListStaffByRole`), `GET /admin/itineraries/:id/notes` + `POST` (contact history; author = actor from auth token, name joined from users), `POST /admin/itineraries/:id/open` (pending→processing CAS), `/:id/close` ({pending,processing}→closed), `/:id/cancel` ({pending,processing}→cancelled, staff-initiated — records cancel_reason + cancelled_at), `/:id/assign` (validate assignee is an active travel_planner; nil = unassign), `GET /admin/itineraries/export` (CSV stream, all matching rows). RBAC: `itinerary.read`→{travel_planner, customer_service} (inbox/detail/notes/planners/export); `itinerary.write`→{travel_planner} (transitions/assign/add-note). **SLA cron wired**: the existing `sla:check` (every 15min) handler now calls `ListBreached` → `SetSLANotified` (CAS exactly-once) → dashboard notification + Brevo email to every travel_planner. Widened `itinerary.UserFetcher`→`UserDirectory` (added `ListStaffByRole`); `user.ServiceInterface` gained `ListStaffByRole`. Live-verified (planner login → inbox/filters/detail/notes/planners/open/re-open-409/assign-invalid-400/close/CSV; cs read=200 write=403; customer 403; SLA breach → notified + email). `quoted`/`deposit_paid`/`confirmed` transitions + quote object deferred to the quote sub-track. Seed `15_staff.sql` (planner + cs users) + `85_itineraries.sql` (assign #1 to planner + 1 crm_note). Integration tests: `TestTransitionStatus_CAS`, `TestCancelByStaff_AcceptsProcessing`, `TestAssign_PersistsAndUnassigns`, `TestAddNote_ListNotes`, `TestListAdmin_FiltersAndJoinsCustomer`, `TestGetByIDAdmin_JoinsCustomer`, `TestSLA_ListBreached_AndNotifiedCAS`.
- [x] Quote builder with **mocked option rates** (CMS rate table, CNY); 30% deposit payment via payment stack — migration `000024_itinerary_quotes` (`option_rates` mocked CMS table + `itinerary_quotes` UNIQUE-per-request, line_items JSONB CNY + presentment total_minor/deposit_minor + fx_rate_used snapshot + quote status `sent|deposit_paid|fully_paid|cancelled`; evolve `payments`: `order_id` DROP NOT NULL + `itinerary_quote_id` + exactly-one CHECK — deposits reuse the payments table per TDD §3.4 line 189). `itinerary` module extended: `SendQuote` (price from option_rates → CNY total → fx.Convert → 30% deposit, snapshot fx_rate_used, request processing→quoted, re-quote replaces via ON CONFLICT, best-effort quote-ready email); `GetQuote` (planner + customer); `PayDeposit` (verify ownership + quoted + quote sent → CreateQuoteIntent; mock auto-finalize seam); `MarkDepositPaid` (worker CAS quote sent→deposit_paid + request quoted→deposit_paid, idempotent); `Confirm` (deposit_paid→confirmed); `RefundDeposit` (fail-closed: gateway.Refund first → CancelQuote → request cancelled). `payment` module extended: `CreateQuoteIntent`/`RefundQuote` + `GetByGatewayRef`/`GetSucceededByItineraryQuoteID` + webhook entity-dispatch (OrderID vs ItineraryQuoteID). `jobs.PaymentFinalizePayload` gained `ItineraryQuoteID`; worker dispatches. Mock-mode seam: worker `MarkSucceededByGatewayRef` advances the pending payment row to succeeded (no webhook in mock). **Endpoints**: `GET /admin/itineraries/option-rates`, `POST/GET /admin/itineraries/:id/quote`, `POST /admin/itineraries/:id/confirm`, `POST /admin/itineraries/:id/refund-deposit` (itinerary.write); `GET /itineraries/:id/quote`, `POST /itineraries/:id/pay-deposit` (customer auth). RBAC: `itinerary.write`→{travel_planner} (send/confirm/refund); `itinerary.read`→{travel_planner, customer_service} (view quote + option-rates). Injected `CheckoutFX`/`QuotePaymentIntenter`/`QuoteRefunder`/`DepositFinalizeEnqueuer` (setters, break the itinerary↔payment cycle). Seed `75_option_rates.sql` (8 mocked rates) + `85_itineraries.sql` (request #5 quoted + sent quote). `00_reset.sql` now truncates the itinerary tables. Integration tests: `TestSendQuote_PricesFromOptionRatesAndConverts`, `TestCreateQuote_ReplacesOnResend`, `TestSendQuote_RejectsNonProcessing`, `TestSendQuote_UnknownOptionKey`, `TestMarkDepositPaid_Idempotent`, `TestConfirm_DepositPaidToConfirmed`, `TestRefundDeposit_CancelQuoteAndRequest`, `TestPayDeposit_OwnershipAndStatus`. **Deferred**: PRD §3.3.2 tiered partial refunds (100/50/25/0% by days-before-arrival — needs partial-refund gateway + policy engine); itinerary PDF (chromedp sub-track).
- [x] **option_rates admin CRUD** (PRD §3.3.2: operator-configured per-option rate table) — full CMS CRUD on the existing `option_rates` table (no migration; schema unchanged). `models`: `OptionRate` gained internal `CreatedAt`/`UpdatedAt` (`json:"-"`); DTOs `CreateOptionRateRequest{OptionKey, RateCNY, Unit, DisplayLabel}` + `UpdateOptionRateRequest{RateCNY, Unit, DisplayLabel}` (validator tags; `option_key` immutable on update — absent from the UPDATE DTO). `itinerary.Repository`: `CreateOptionRate`/`UpdateOptionRate`/`DeleteOptionRate` (shared `optionRateCols`/`scanOptionRate`; UPDATE excludes `option_key` so renames can't orphan historical quote snapshots; `pgx.ErrNoRows`→`ErrNotFound`). `itinerary.Service`: `CreateOptionRate` (regex guard `^[a-z0-9][a-z0-9_-]*$` → `ErrInvalidOperation` for a clear 400 before SQL; DB CHECK backstop) + `Update`/`Delete` (NotFound mapping). **Endpoints**: `POST /admin/itineraries/option-rates` (201), `PUT /admin/itineraries/option-rates/:id` (200), `DELETE /admin/itineraries/option-rates/:id` (204); the existing `GET /admin/itineraries/option-rates` (list) stays planner-readable. **RBAC split**: list under `itinerary.read` (travel_planner + customer_service — planners read rates to quote); write under `settings.manage` (super_admin only — pricing is a business decision, matching the shipping-tiers precedent). Write routes registered on `adminItinRead` with a route-level `RequirePermission(PermSettingsManage)` (NOT a separate group — a sibling group at the same prefix leaked its middleware to the GET route, the same Fiber v2 `Group("").Use()` prefix-attach behavior from the `/media` fix). Duplicate `option_key` → 409 (handler probes the `option_rates_option_key_key` constraint name). Seed: added `superadmin@jingdezhen.test` (super_admin, id ...005) to `15_staff.sql` for live-test RBAC (2FA enrollment completed via the pending-enroll API flow; `admin@jingdezhen.test` at ...001 is a preserved fixture not in the seeds). Live-verified end-to-end: super_admin POST→201, PUT→200 (option_key unchanged), DELETE→204, re-delete→404, bad-key→400, duplicate→409; planner GET→200 / POST→403. Tests: `TestCreateOptionRate_OK`, `TestCreateOptionRate_BadKey` (regex), `TestCreateOptionRate_DuplicateKey`, `TestUpdateOptionRate_KeyImmutable`, `TestUpdateOptionRate_NotFound`, `TestDeleteOptionRate_OK`, `TestDeleteOptionRate_NotFound`. All 16 packages pass under `-race`.
- [x] chromedp PDF adapter (TDD §12 settled) — `pkg/adapters/pdf/` (`Generator` interface + `NoopGenerator` [dev, returns ErrPDFUnavailable] + `ChromedpGenerator` [remote-allocator to a `chromedp/headless-shell` sidecar at `ws://chromedp:9222` over the DevTools protocol; the Go binary never bundles Chromium]). `internal/platform/pdftmpl/` (embedded HTML templates + `RenderCertificate`; self-contained inline CSS, `<img>` fetched over the network at render time). First consumer: certificate PDF — `certificates.pdf_key` (nullable column from 000019, now populated) + `GET /certificates/:code/pdf` (public; 302→`storage.PublicURL(pdf_key)`, `?download=1` forces attachment, 404 when not yet generated). `IssueForProduct`/`Regenerate` enqueue `pdf:generate{kind:certificate, entity_id, locale}` (best-effort, async); the worker renders → `storage.Put(pdf/certificates/<code>.pdf)` → `certRepo.SetPDFKey`. `jobs.PDFGenerate` evolved from a payload-less runNoop to a payload'd handler (`PDFGeneratePayload{Kind, EntityID, Locale}`) + `EnqueuePDFGenerate`. Config: `PDF_MODE` (local|chromedp), `CHROMEDP_URL`, `PDF_BASE_URL` (origin the sidecar fetches the QR `<img>` from). Compose: `chromedp` sidecar service (no host port; only the worker reaches it) + worker env. Live-verified both paths: local (worker logs 'local mode, skipping', pdf_key stays NULL, endpoint 404s 'PDF not yet generated') + chromedp (worker 'rendered cert=1 → pdf/certificates/JDZ-000001.pdf (31056 bytes)', valid PDF v1.4 1-page, pdf_key populated, endpoint 302→stored URL). Build note: chromedp v0.16.0 requires Go 1.26 → bumped Dockerfile `golang:1.25-alpine`→`1.26-alpine`. **Pre-existing quirk noted**: the `/media` static mount is auth-protected in the current build (seeded media also 401s unauth'd) — the public cert PDF endpoint redirects there; a public static-serving fix (register `app.Static` before the auth groups, or a dedicated public-media handler) is a separate follow-up. The itinerary quote/confirmation PDF is the next consumer (needs `itinerary_quotes.pdf_key` column + a quote template — mechanical once the engine + scaffolding are in place).
- [x] **`/media` public static-serving fix** — root cause: Fiber v2 `app.Group("").Use(JWTMAuth)` (the authed `checkoutGroup` for /orders, /itineraries, /checkout) attaches its middleware at the radix root, so it **leaks to every route registered AFTER it**. This silently auth-protected two public mounts: `engageGroup` (`/engage`, also wrongly 401) + `app.Static("/media")` (product gallery + cert/itinerary PDFs, registered after `SetupRoutes` in main.go). Routes registered *before* the leak (`/fx/rates`, `/certificates/:code`) were unaffected — so the bug was masked. Fix: (1) moved `engageGroup` to before `checkoutGroup` in `router.go`; (2) moved `app.Static` to before `api.SetupRoutes` in `main.go`; (3) added a WARNING comment at `checkoutGroup` ('Do not add public routes below here') + an explanatory comment at `engageGroup`/`app.Static` documenting the Fiber v2 `Group("").Use()` ordering gotcha (TDD §4.3). No new config, no URL changes, no migration. Live-verified: `/media/pdf/certificates/JDZ-000001.pdf` → 200 (unauth, valid PDF v1.4), `/engage` → 200 (returns 5 activities), `/certificates/:code/pdf` → 302 public, while `/orders` + `/itineraries` stay 401 unauth + 200 with a token. All 16 packages pass under `-race`. Regression guard: the WARNING comment at the empty-prefix group.
- [x] Itinerary quote PDF (TDD §12, M3 #4 follow-up) — second consumer of the chromedp adapter. Migration `000025_itinerary_quote_pdf` (`itinerary_quotes.pdf_key TEXT`, nullable; mirrors `certificates.pdf_key`). `pdftmpl/itinerary_quote.html.tmpl` (A4 branded: crest + trip details grid [arrival/duration/travelers/pace/interests/services] + priced line-items table + totals [CNY subtotal, presentment total, 30% deposit, balance] + FX-rate note + 14-day-validity footer). `pdftmpl.RenderItineraryQuote(req, quote, locale)` parses the `line_items` JSONB + formats money (minor→major) + dates. `SendQuote` enqueues `pdf:generate{kind:"itinerary_quote", entity_id: quote.ID, locale: request.locale}` best-effort after the processing→quoted transition (nil-safe; logged on failure). `itinerary.Repository.SetQuotePDFKey` (UPDATE; ErrNotFound on miss). `CreateQuote`'s ON CONFLICT clears `pdf_key` on re-quote (stale PDF invalidated). Worker `jobServer.PDFGenerate` switch: `case "itinerary_quote"` → `renderItineraryQuotePDF` (load quote → load request → render → `storage.Put(pdf/itineraries/<req>.pdf)` → `SetQuotePDFKey`; NoopGenerator/local mode skips + 404s). **Endpoints**: `GET /itineraries/:id/quote/pdf` (customer, ownership-checked) + `GET /admin/itineraries/:id/quote/pdf` (planner); 302→`store.PublicURL(pdf_key)`, `?download=1` attachment, 404 'PDF not yet generated' when NULL. `itinerary.NewHandler(service, store)` (new store arg for pdf_key→URL). `certPDFEnqueuer`→`pdfEnqueuer` (generic; cert + itinerary share it). Live-verified both paths: chromedp (worker 'rendered itinerary_quote=2 → pdf/itineraries/1.pdf (50955 bytes)', valid PDF v1.4 1-page, pdf_key populated, customer+planner downloads 302, `?download=1` attachment `itinerary-1.pdf`, followed redirect → valid 50955-byte PDF) + local (worker 'local mode, skipping', pdf_key NULL, 404 'PDF not yet generated'). Tests: `TestSendQuote_EnqueuesPDF` (mock enqueuer captures kind/entityID/locale), `TestSendQuote_NilPDFEnqueuerNoPanic`, `TestSetQuotePDFKey_Persists` (NULL→set→re-quote clears), `TestSetQuotePDFKey_NotFound`. All 16 packages pass under `-race`. The confirmation step reuses the same PDF (the quote doc serves as the confirmation; PRD §3.3.2 line 269 content is satisfiable from quote+request data at send time). The `/media` public static-serving quirk is now fixed (see the `/media` static-serving fix item below).
- [ ] Chatbot: WebSocket chat + Qwen3.5 + live handoff to agent console (minimal viable)
- [ ] WhatsApp Business messaging (Meta Cloud API) — *may trail post-MVP*

## M4 — Compliance, Dashboard & Hardening

- [x] Cookie consent banner backend (consent records); analytics gated on consent
  — consent module (M1) already records `cookie_analytics`; the analytics service's consent gate (commit below) reads the latest granted record by IP hash.
- [x] In-house analytics: event endpoint + GeoLite2 geolocation + PostgreSQL
  — **migration `000026_analytics`** (`analytics_events` + `analytics_daily`); `pkg/adapters/geoip/` (`Lookup` interface + `Noop`→'ZZ' dev default + `MaxMind` GeoLite2-Country reader, env-flip `GEOIP_MODE`); `internal/modules/analytics/` (handler→service→repository: `POST /analytics/events` public + consent-gated via `consent.Service.GetConsentStateForIP`; visitor_hash = `hex(HMAC(dailyRotatingKey, IP+UA))`, no raw IP stored; GeoLite2 country resolved at ingest, 'ZZ' on miss; `analytics:rollup` nightly job wired — pageviews/events/visitors rolled up idempotently into `analytics_daily` via `INSERT…ON CONFLICT DO UPDATE SET value=excluded.value`). Fiber `limiter` (60/min/IP, TDD §333) on the route group. Config: `ANALYTICS_HMAC_KEY`, `GEOIP_MODE`, `GEOLITE2_DB_PATH`. Unit tests (consent gate, visitor_hash stability/rotation, pageview nil-name, rollup) + integration tests (consented insert + props JSONB round-trip, refused/never-consented drop with zero rows, GeoIP GB resolution, rollup aggregation + idempotency, empty-day no-op). Live-verified (build/vet/lint/-short green; integration pending Docker on the dev box — CI runs them via testcontainers).
- [x] Dashboard APIs: traffic, sales, itinerary funnel; CSV export
  — `internal/modules/analytics/` Phase B: `analytics_dashboard_{repo,service,handler,scan}.go`. Three `GET /admin/analytics/{traffic,sales,funnel}` endpoints (RBAC `dashboard.view`→{ecommerce_operator, customer_service}). **Reads live source tables** (analytics_events / orders / order_items / itinerary_requests) — NOT analytics_daily (MVP volume; live is cheap + always-correct, no "today not rolled up" gap; analytics_daily stays wired for a future scale-out switch, TDD §12). **Traffic** (PRD §3.4.2): totals (pageviews/distinct visitors/named events) + by_country + by_path (top content) + by_locale + zero-filled daily series, all from analytics_events. **Sales**: GMV = Σ subtotal_cny (merchandise, excludes shipping) over realized orders only (status IN paid|shipped|completed — cancelled/refunded excluded); by_currency + by_country (address->>'country') + by_product (JOIN skus/products + en-US title fallback) + by_artist (products.artist_id NULL→'Unknown') + daily series. **Funnel**: views (analytics_events name=`itinerary_form_view`) → submitted (itinerary_requests submitted_at in range) → confirmed (cohort: status='confirmed', no confirmed_at column) + conversion rates (pct, 2dp, 0 on zero-denom) + daily series. Date-range filter: `?range=day|week|month|quarter|year` (server computes [now-start,now)) or `?from=&to=` (YYYY-MM-DD, inclusive), default 30d, **cap 366d** (absorbs the `year` preset + inclusive-to normalization). CSV export via `?format=csv` on each endpoint (one query path per report; `text/csv` + `Content-Disposition: attachment`). Breakdown lists capped at 100 rows; series zero-filled in Go (no SQL gaps). Integration tests seed a 3-day window (orders incl. cancelled/NULL-artist; itinerary confirmed/pending; analytics events) + assert totals/breakdowns/series/CSV + zero-denominator conversions. Unit tests for `parseRange` (presets, explicit, inverted, bad dates, cap).
- [x] Audit log for sensitive admin actions
  — migration `000027_audit_log` (`audit_log` table: `actor_id UUID NULL FK ON DELETE SET NULL`, `actor_ip_hash`, `action`, `entity_type`, `entity_id VARCHAR`, `detail JSONB`, `created_at`; 3 indexes). `internal/modules/audit/` (handler→service→repository + `Helper` wrapping `Logger` with Fiber-context actor extraction). **28 sensitive endpoints instrumented**: content transitions (approve/reject/unpublish × 4 modules), deletes (product/sku/ceramic-story/activity/artist/media/shipping-tier/option-rate), role assign, order refund, itinerary cancel/assign/confirm/refund-deposit, GDPR erasure. Each handler gets an `audit *audit.Helper` field + `SetAuditLogger` setter (wired in main via a loop). `audit.ActionForTransition(to)` maps content-workflow target status → audit action (shared by the 4 content modules' `adminTransition`). `actor_ip_hash` = HMAC-SHA256(CONSENT_HMAC_KEY, IP) — same short-term audit purpose as consent, one fewer secret. Read endpoint `GET /admin/audit-log` (RBAC `settings.manage` — super_admin only, stricter than `dashboard.view`) with filters (`actor_id`/`action`/`entity_type`/`entity_id`/`from`/`to`/`range`) + pagination + `?format=csv`. `parseRange` extracted to `pkg/utils/range.go` (shared by analytics dashboard + audit). `audit.Logger` interface + `NoopLogger` for tests/worker. Integration tests: Log inserts row with actor + IP hash + detail JSONB; no-actor → NULL actor_id + NULL ip_hash; List filters + paginates; CSV export; JSON paginated; date-range filter (valid + inverted→400). Unit tests for `ParseRange` (presets, explicit, inverted, bad dates).
- [x] JWT blocklist for deleted-user token invalidation (M4 security pass, TDD §5.1 stopgap)
  — `pkg/adapters/tokenblocklist/` (`Blocklist` interface + `RedisBlocklist` + `NoopBlocklist`, mirroring the geoip/pdf adapter pattern). **Redis denylist keyed by `user_id`** (TDD §12): on GDPR erasure, `privacy.Service.DeleteAccount` calls `blocklist.Revoke(ctx, userID, MaxAccessTokenTTL)` best-effort (log on Redis error, never blocks erasure) — writes `jwt:revoked:<userID>` with TTL = **30d** (the max access-token lifetime; by expiry every outstanding token has self-expired, so the key can be pruned without missing a revocation). `JWTMAuth` consults the blocklist in `SuccessHandler` **after** signature+expiry pass: revoked user_id → 401 “Token revoked” even with a valid signature. **Fail-open on Redis outage** (read path returns `false, nil` on error) so a Redis outage never locks out every authenticated user; a deleted user's token lingers only during the outage + self-expires ≤30d, and login is already blocked by `is_active=false`. **No migration, no schema change** (first auth-path Redis consumer; the previously-unused `redisClient` in main is now wired). `JWTMAuth(secret, bl)` signature changed at all 10 router call sites; `SetupRoutes` + `privacy.NewService` take a `Blocklist`/`TokenRevoker` param (Noop for worker/no-Redis). `tokenblocklist.MaxAccessTokenTTL` is the single source of truth for the 30d access-token lifetime (`user_service.generateAuthResponse` references it). Integration tests: `TestDeleteAccount_RevokesOutstandingToken` (login→token works→`DeleteAccount`→SAME token now 401; blocklist reports revoked; user anonymized), `TestDeleteAccount_NilRevokerSkipsRevocation` (nil revoker doesn't break erasure). Middleware unit/integration: revoked→401, non-revoked→200, nil blocklist→200, fail-open on closed-Redis→200, missing token→401. Blocklist unit/integration: Revoke+IsRevoked, idempotent, TTL expiry, nil-client noop, fail-open on unreachable Redis. **Stopgap only** — refresh-token rotation (TDD §5.1) replaces this with short-lived access + per-refresh-token denylist (TTL drops 30d→minutes) once the frontend lands (rotate-on-401 flow is untestable without it). Per-token (`jti`) revocation + password-change/role-demotion revocation are future extensions; the `Blocklist` interface is shaped to accommodate them.
- [x] Backup-code rate-limiting on `/auth/2fa/verify` (M4 security pass, TDD §333)
  — **Two-layer brute-force defense** on the three 2FA code routes (`/auth/2fa/verify` + `/auth/2fa/pending-enroll` + `/auth/2fa/pending-confirm`). **Layer 1 (per-IP Fiber in-process `limiter`, 5/min/IP)** attached at route registration in `router.go` (NOT `.Use()` on the `/auth` group — scoped to just these three routes; login/signup/etc. intentionally NOT throttled in this pass). Stops a single-IP brute-forcer cold (one TOTP window = ~2-3 attempts; 5/min/IP is generous for a legit user who mistypes). **Layer 2 (per-userID Redis failed-attempt lockout)** in `user.Service.Complete2FALogin` via new `pkg/adapters/ratelimit/` (`AttemptTracker` interface + `RedisAttemptTracker` + `NoopAttemptTracker`, mirroring tokenblocklist). Keys: `2fa:fail:<userID>` (INCR + EXPIRE 15min on first failure) and `2fa:lock:<userID>` (SET EX 15min when counter hits **MaxFailures=5**). `IsLocked` = `EXISTS(lock)` — the lock has its own TTL independent of the counter, so it survives the counter window elapsing. The lockout is keyed by **userID** (NOT the pending token) so re-login doesn't reset the counter; a successful verify `Reset`s it (a user who mistyped then got it right doesn't carry a near-threshold counter forward). **Fail-open on Redis outage** (IsLocked returns false on Redis error; RegisterFailure/Reset are best-effort) — a Redis outage never locks EVERY 2FA user out (would be a self-DoS); the attack window is bounded by the 5-15min pending-token TTL + the per-IP limiter stays active. `user.NewService` takes an `attemptTracker` param (Noop if nil; Redis-backed in serve mode); `NewRedisAttemptTracker(redisClient)` wired in `main.go` alongside `tokenBlocklist`. `ErrTooManyAttempts` added to `models/errors.go`; handler maps it → **429** `"Too many failed attempts, try again later"`. **DoS trade-off**: an attacker who knows the password can lock a legit user out of 2FA for 15 min by firing 5 bad codes — accepted (standard for 2FA brute-force defense; 15-min window is short; the user retries later). A bad pending token short-circuits BEFORE the lockout counter (a brute-forcer can't inflate a random user's counter without the pending token that proves they know the password). Tests: `TestComplete2FALogin_LocksAfterMaxFailures` (MaxFailures-1 bad→ErrInvalidCredentials; MaxFailures-th bad→ErrInvalidCredentials; NEXT attempt with CORRECT code→ErrTooManyAttempts), `TestComplete2FALogin_SuccessResetsCounter`, `TestComplete2FALogin_BadPendingTokenShortCircuits`. Ratelimit adapter: 5-failure-lock, success-resets, lock-survives-counter-TTL, fail-open-on-closed-Redis, nil-client-noop, noop-tracker-never-locks. Build/vet/lint(-short -race)/swag-check green.
- [ ] Rate limiting (login/signup/password-reset + global), webhook signature verification, security pass
- [ ] k6 load tests (thresholds per PRD §2.4.3); backup/restore drill

## Testing & CI (PRD §2.4 — build up alongside milestones)

- [x] testcontainers-go harness — `internal/testutil`: one shared Postgres container (`postgis/postgis:16-3.5-alpine`, matches dev compose) + one shared Redis container per `go test` run (sync.Once); each test gets an isolated per-test DB (CREATE → migrate all 21 migrations via golang-migrate's Go API driven by an embedded FS (`internal/migrations/embed.go`, `//go:embed *.up.sql *.down.sql`) → DROP on cleanup) and a per-test Redis DB index (FLUSHDB on cleanup). `NewDBPool(t)` returns a ready `*pgxpool.Pool` that drops straight into any repo's `NewRepository(pool)`. `NewRedisClient(t)` returns a `*redis.Client`. Hermeticity verified (writes-in-A / sees-nothing-in-B subtests). `-short` skips integration tests (fast inner loop). `make test` (all, -race) / `make test-unit` (-short, no Docker) / `make test-integration`.
- [x] Example integration tests — `internal/modules/order/order_service_test.go`: the TDD §11 priority (order state machine + atomic stock decrement): `TestCreateOrder_AtomicStockDecrement` (happy: exact-stock → 0; failure: oversell → ErrConflict, stock unchanged, no partial order), `TestCreateOrder_RollsBackOnAnyItemInsufficient` (multi-item: second item out of stock rolls back the FIRST item's decrement too — the whole-tx invariant), `TestSetCancelled_RestoresStock` (cancel restores stock atomically), `TestTransitionStatus_IdempotentMarkPaid` (replayed webhook → ErrConflict not a second transition; absent order → ErrNotFound). Smoke tests in `internal/testutil/testutil_test.go` prove the harness itself (all migrations applied, unseeded, isolation).
- [x] RBAC middleware matrix — `internal/api/middleware/auth_test.go` (unit, no DB): table-driven `TestRequirePermission_RBACMatrix` covers every permission in `permissionRoleGate` × every staff role + super_admin bypass + multi-role ANY-match + no-roles-403 + unknown-perm-403 + `PermProductPublish` (absent from the gate → super-admin-only by design). `TestRequireRole` locks the older single-role gate. A regression here is a privilege escalation — the routing-layer security boundary.
- [x] Payment webhook idempotency (DB side) — `internal/modules/payment/payment_repository_test.go` (integration): `TestUpsertWebhook_IdempotentReplay` (replay → inserted=false, one row), `TestUpsertWebhook_DistinctKeysAreDistinctRows` (distinct keys → distinct rows; guards against over-broad dedupe), `TestUpsertWebhook_ConcurrentReplaysOneWins` (8 goroutines racing the same key → exactly one winner, one row, no errors — the double-charge guard under real concurrency), `TestUpsertWebhook_UpdatesIntentStatus` (first-seen succeeded event advances the pending intent → succeeded; replay must not re-advance), `TestSetRefunded_CAS` (refund is a CAS on status='succeeded' — double-refund → ErrConflict). Pairs with the existing `payment_service_test.go` unit test (control flow): the unit test proves the service no-ops on a replayed event; this proves the DB enforces it via `idempotency_key UNIQUE` + the `xmax=0` probe.
- [x] i18n slug resolution + tag filter — `internal/modules/product/product_repository_test.go` (integration): `TestFindPublishedBySlug_LocaleScoped` (two locales share the same slug text → resolve to DIFFERENT products; a slug absent in the requested locale → 404, no cross-locale fallback), `TestFindPublishedBySlug_OnlyPublished` (draft/rejected translations invisible on the public read path), `TestFindAllPublished_TagFilter` (single-tag + multi-tag ANY-match + unknown-tag-returns-zero; the EXISTS subquery written in the tags feature, previously only manual-verified), `TestFindAllTagsInUse_OnlyPublishedProducts` (facet list only counts tags on ≥1 PUBLISHED product — a tag on a draft-only product must not appear, would leak unpublished content).
- [x] Certificate uniqueness + provenance atomicity — `internal/modules/certificate/certificate_repository_test.go` (integration): `TestNextCode_GeneratesUniqueCodes` (collision-retry probes past an existing code), `TestIssue_ManyCodesAllDistinct` (50 issues → 50 distinct JDZ-<6-base32> codes; catches a broken code() modulo bias or missing retry), `TestIssue_ProductIDUnique` (one cert per product — second issue for same product fails), `TestIssue_AppendsCreatedProvenance` (the `created` provenance row is in the same tx as the cert insert — a cert with no `created` row is a broken chain), `TestGetByCode_LoadsProductAndProvenance` (public QR-page JOIN path + nonexistent code → ErrNotFound).
- [x] GDPR erasure preserves order history — `internal/modules/privacy/privacy_repository_test.go` (integration): `TestAnonymizeUser_PreservesOrderHistory` (after erasure: user PII nulled + is_active=false + deleted_at set, BUT the order still exists + still references the same user_id — NO ACTION FK, no cascade), `TestAnonymizeUser_IdempotentOnReErase` (second erasure → ErrAccountDeleted, not a no-op), `TestAnonymizeUser_NonexistentUser` (→ ErrNotFound). Pins the migration's NO ACTION + the code's anonymize-in-place behavior together against real PG.
- [x] **GitHub Actions CI pipeline** (PRD §2.4.1) — `.github/workflows/ci.yml`: five jobs on `ubuntu-24.04` (Docker preinstalled for testcontainers), triggered on PR + push to `main` (+ `workflow_dispatch`): **lint** (`golangci-lint v2.12.2` via `go install` (built with Go 1.26 from `go-version-file`; required because the prebuilt v1.64 binary was built with Go 1.24 and refuses to lint our `go 1.26` module), `backend/.golangci.yml` v2 config — gofmt/goimports/govet/errcheck/staticcheck/ineffassign/unused/misspell/gosimple; stricter than `go vet`; + `actionlint` on workflows + `make swag-check` spec-freshness gate) → **unit** (`go test -race -short -coverprofile`, no Docker, uploads coverage artifact) → **integration** (`go test -race`, testcontainers-go real PG+Redis, `TESTCONTAINERS_RYUK_DISABLED=true`, `timeout-minutes: 20` for cold image pulls) → **build** (`docker/build-push-action`, `push: false`, validates the Dockerfile compiles the binary; `needs: [unit, integration]` so a broken tree doesn't waste the build) → **security** (`govulncheck` hard gate — call-graph-validated reachable vulns block; `osv-scanner` advisory `continue-on-error` — broader transitive-dep scan that reports but doesn't block on unfixed upstream CVEs; both installed directly via `go install` since their third-party action wrappers were broken for v2). Concurrency group cancels stale runs on a new push. No secrets required (PR-stage; deploy stage deferred until ACR registry + VPS SSH secrets land). `setup-go@v5` reads `go-version-file: backend/go.mod` (Go 1.26) + `cache-dependency-path: backend/go.sum`. **Lint cleanup landed**: `gofmt`/`goimports` applied across the tree (119 auto-fixes); removed dead `mapKeysToSlice`; `depositFraction` float → `depositPercent` int const used in `roundDeposit`; `ActivateUser` dead `:= &User{}`; dead `n++` in `ListAdmin`; `sch.Register` errcheck → `mustRegister` helper (panics on bad cron spec — a programmer error); `.golangci.yml` excludes idiomatic deferred `Close`/`Rollback`/WS-write errchecks. **Dep bumps landed** (govulncheck-driven): `gofiber/fiber/v2` v2.52.9→v2.52.12, `golang.org/x/text` v0.37→v0.40, `golang.org/x/crypto` v0.51→v0.54, `golang.org/x/net` v0.53→v0.54, `mapstructure/v2` →v2.4.0 — govulncheck now reports **0 reachable vulnerabilities**. `.github/dependabot.yml`: weekly Monday updates for `gomod` (backend) + `github-actions`. Local-verified: lint clean, unit pass, integration 16/16 packages green under `-race`, Docker build succeeds, govulncheck 0 reachable, osv-scanner 6 non-reachable transitive (advisory). **CI pipeline fully green on `main`** (run 30969012675, all 5 jobs ✓).
- [x] **OpenAPI/Swagger docs — catalog pilot** (PRD §2.4.2) — `swaggo/swag` v1.16.4 (Swagger 2.0; v2.x OpenAPI 3.0 migration deferred until v2 stable) + `swaggo/fiber-swagger` v1.3.0. General API info in `cmd/api/main.go` package doc (`@title`, `@version 1.0`, `@BasePath /`, `@securityDefinitions.apikey BearerAuth`). Annotations on the 4 public catalog handlers in `product_handler.go`: `GetProducts` (`GET /catalog/products`), `GetProductBySlug` (`GET /catalog/products/{slug}`), `GetCategories` (`GET /catalog/categories`), `GetTags` (`GET /catalog/tags`) — each with `@Param`/`@Success`/`@Failure` referencing existing `models.*` DTOs (Product, SKU, PaginatedResponse, TagWithCount, ErrorResponse) so the DTOs stay the single source of truth (no duplication). Generated spec in `internal/docs/` (`docs.go` + `swagger.json` + `swagger.yaml`, committed so CI drift-detects). UI served at `GET /admin/swagger/*` behind `JWTMAuth` (any authenticated staff; public would expose the API surface). Makefile targets: `make swag` (regenerate), `make swag-fmt` (normalize annotations), `make swag-check` (CI gate: regenerate + `git diff --exit-code`). CI lint job runs `make swag-check`. **Live-verified**: `/admin/swagger/index.html` → 200 with JWT / 401 without; `/admin/swagger/doc.json` → 200 with the 4 catalog paths + title/version. Remaining: annotate the other ~188 routes (admin CMS, cart, order, payment, itinerary, auth, etc.) incrementally by module.

## Notes

- Existing repo had uncommitted local modifications; deletions were done on the filesystem — **review `git status` and commit the deletions**.
- `notification` module kept; PRD-aligned notification types (order status, low-stock, itinerary status, chat handoff, content approval) to be added as modules land.
- User schema review (PRD §8, item 7) partially done during this cleanup; the remaining alignment happens in M0 tasks above.
